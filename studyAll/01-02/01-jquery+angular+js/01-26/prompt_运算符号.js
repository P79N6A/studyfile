prompt("输入");弹出输入框，并返回输入值，不输入返回null;

'数学运算符号';
+,-,*,除号（/）,%
加,减,乘,除,模

'前增减，后增减';和java相反;
a++; ++a;

关系运算符号;
==,>=,<=,!=;

'逻辑运算符号';
|| && !

'流程控制switch'
var flag="1.1";
switch(flag){
	default:
		alert();
	case 'a':
		alert();
	case '1.1':
		alert();
}
