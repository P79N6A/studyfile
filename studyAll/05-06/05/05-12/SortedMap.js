//SortedMap接口的作用;
'SortedSet','是TreeSet的'实现'接口'，那么'此接口可''进行排序接口'

'SortedMap是排序操作'，'TreeMap'类，'是可以排序的'
方法{
	comparator()返回'比较器对象'
	firstKey()返回'第一个元素的key'
	headMap(K toKey)返回'小于指定key的部分集合'
	lastKey()返回'最后一个元素Key'
	subMap(K fromKey,K toKey)返回'指定集合的起点和终点'
}